(ns clojureboy.mmu)

(defn rb
  "Read 8-bit byte from MMU."
  [addr]
  nil)

(defn rw
  "Read 16-bit word from MMU."
  [addr]
  nil)

(defn wb
  "Write 8-bit byte to MMU."
  [addr value]
  nil)

(defn ww
  "Write 16-bit word to MMU."
  [addr value]
  nil)
